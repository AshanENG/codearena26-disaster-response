# Validation record

Current actual-clone results are recorded in progress.md. Nine tests and production build passed; real persistence and Gemini remain blocked on local configuration. Built-app browser checks passed; Vite development optimization is blocked by the Codex sandbox's ancestor-directory access restrictions.

Use npm test, npm run build, npm run test:persistence, and npm run smoke:gemini -- "ABSOLUTE_IMAGE_PATH". Review README for setup and docs/deployment.md for the tested local same-origin arrangement. Do not carry earlier separate-worktree results forward as new verification.
